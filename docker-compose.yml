version: "3.8"

services:
  slurm:
    image: giovtorres/docker-centos7-slurm:23.02.6
    hostname: slurmctl
    container_name: slurmctl
    stdin_open: true
    tty: true
    working_dir: /pyslurm
    environment:
      PYTHON: "3.9"
    volumes:
      - ${PWD}:/pyslurm
